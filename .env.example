# Chat Application Environment Variables
# Copy values from this file and adjust them for your environment

# ===== BACKEND (.env) =====
# MongoDB connection URI
MONGO_URI=mongodb://localhost:27017/chatdb

# JWT secret key (use a long, random string for production)
JWT_SECRET=replace_with_a_secure_random_string

# Server port
PORT=5000

# ===== FRONTEND (.env.local) =====
# Backend API base URL
VITE_API_BASE_URL=http://localhost:5000/api

# Socket.IO server URL
VITE_SOCKET_URL=http://localhost:5000

# Instructions:
# 1. Backend: Copy to chat-app-backend/.env and update MONGO_URI and JWT_SECRET
# 2. Frontend: Copy frontend vars to chat-app-frontend/.env.local
